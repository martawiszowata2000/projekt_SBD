@page
@model projekt_SBD.Pages.Wizyty.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Wizyta[0].DataGodzina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Wizyta) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataGodzina)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WizytaId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WizytaId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WizytaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
