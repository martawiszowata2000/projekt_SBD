@page
@model projekt_SBD.Pages.Wizyty.DeleteModel

@{
    ViewData["Title"] = "Usuń";
}

<h1>Usuń</h1>

<h3>Czy napewno chcesz to usunąć ?</h3>
<div>
    <h4>Wizyta</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Data i godzina:
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Wizyta.DataGodzina)
        </dd>
        <dt class="col-sm-2">
            Stomatolog:
        </dt>
        <dd class="col-sm-10">
            @Model.GetStomatolog(Model.Wizyta.StomatologId)
        </dd>
        <dt class="col-sm-2">
            Asystent:
        </dt>
        <dd class="col-sm-10">
            @Model.GetAsystent(Model.Wizyta.StomatologId)
        </dd>
        <dt class="col-sm-2">
            Pacjent:
        </dt>
        <dd class="col-sm-10">
            @Model.GetPacjent(Model.Wizyta.StomatologId)
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="Wizyta.WizytaId" />
        <input type="submit" value="Usuń" class="btn btn-danger" /> |
        <a asp-page="./Index">Powrót</a>
    </form>
</div>
