@page
@model projekt_SBD.Pages.Asystenci.IndexModel

@{
    ViewData["Title"] = "Asystenci";
}

<h1>Asystenci</h1>

<p>
    <a asp-page="Create">Nowy profil asystenta</a>
</p>
<form method="post" class="row g-8">
    <div class="col-auto">
        <input asp-for="Wyszukiwanie" class="form-control" id="Szukanie" placeholder="Imię, nazwisko...">
    </div>
    <div class="col-auto">

        <button class="btn btn-primary" type="submit">Szukaj</button>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                Nazwisko
            </th>
            <th>
                Imię
            </th>
            <th>
                Drugie imię
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Asystent) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwisko)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Imie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DrugieImie)
            </td>

            <td>
                <a asp-page="./Edit" asp-route-id="@item.AsystentId">Edytuj</a> |
                <a asp-page="./Details" asp-route-id="@item.AsystentId">Szczegóły</a> |
                <a asp-page="./Delete" asp-route-id="@item.AsystentId">Usuń</a>
            </td>
        </tr>
}
    </tbody>
</table>
